services:
  - type: web
    name: invoice-ocr-app
    env: python
    buildCommand: |
      ./render-build.sh
      pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
